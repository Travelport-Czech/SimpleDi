language: php

php:
  - '5.4'
  - '5.5'
  - '5.6'
  - '7.0'
  - '7.1'
  
before_script:
  - composer self-update

install:
  - composer install --no-interaction --prefer-source

script: echo $COMPOSER_BIN_DIR && $COMPOSER_BIN_DIR/phpunit tests
